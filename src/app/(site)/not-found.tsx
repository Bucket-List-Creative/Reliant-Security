import { NotFoundContent } from "@/components/sections/NotFoundContent";

// Renders inside the (site) layout, so the nav and footer stay in place.
export default function SiteNotFound() {
  return <NotFoundContent />;
}
